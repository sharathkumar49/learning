"""
LeetCode 863: All Nodes Distance K in Binary Tree (Google, Facebook) [One-liner Version]
Given the root of a binary tree, a target node, and an integer k, return all the values of the nodes that have a distance k from the target node (one-liner approach).
"""
from typing import Optional, List

def distance_k_oneliner(root: Optional['TreeNode'], target: 'TreeNode', k: int) -> List[int]:
    # Not practical for interview, but for completeness
    return []  # Not implemented
